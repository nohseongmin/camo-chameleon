# CAMO CHAMELEON 블루프린트

## 한 줄 정의
쇼츠/스트리밍 시청자가 **다운로드 없이 브라우저에서 즉시**, 배경색을 스포이트로 찍어 카멜레온 몸에 칠하고 "술래 스캔"을 통과하는 위장 숨바꼭질 게임.

## 가정
- 원작 **MECCHA CHAMELEON**(LEMORION, 2026-06, 스팀 유료·3D·실시간 멀티, 한 달 1,500만 장)의 **핵심 재미 1개 = "그려서 배경에 녹아든다"**만 증류한다. 3D·실시간 멀티·계정은 전부 OUT.
- 술래는 사람/AI가 아니라 **픽셀 차이 기반 위장점수 알고리즘**으로 대체(백엔드 불필요). "얼마나 잘 녹아들었나"를 실제로 계산해 판정한다.
- 타겟 디바이스는 모바일 세로(쇼츠 시청자) 우선, 데스크톱 겸용.
- 배포는 vs-arena와 동일하게 **GitHub Pages**(정적, 서버 0).
- 바이럴 훅 = **시드 공유**("seed 12345에서 87% 나왔다, 넘어봐"). 점수는 플레이어 실력이라 재현 불가하지만 *장면(scene)*은 시드로 재현된다.

## 시장성 & 경쟁
- **MECCHA CHAMELEON**(원작): 스팀 ₩6,550, 3D, 실시간 멀티, 스트리밍 바이럴. **약점 = 유료 + 설치 필요 + PC.** → 쇼츠로 본 사람이 "바로 해볼" 무료 웹 진입로가 비어 있음.
- **Poki 등 웹 게임 포털**: "meccha chameleon" 검색 트래픽 유입 중이나 원작의 라이트/클론 위주. 우리는 *고유 알고리즘 술래 + 시드 공유*로 차별.
- **Gartic Phone / drawing 파티게임**: 그림+소셜이지만 "위장" 메커닉은 없음.
- **결론**: 원작이 수요를 이미 검증(1,500만 장). 직접 경쟁하는 "무료 웹 싱글 + 알고리즘 술래" 버전은 희소 → 롱테일 검색·쇼츠 유입 노림.

## BM 매칭
- **선택: 광고(경량) → 규모 후 코스메틱.** 근거: 반복 소비하는 캐주얼 게임(광고 BM 표에 해당). vs-arena에서 이미 GoatCounter+애드슬롯 패턴 보유 → 동일 적용. **왜 돈이 되나**: 쇼츠 유입 트래픽 규모 자체가 광고 인벤토리. **언제 과금**: DAU 확보 후 애드슬롯 활성화, 그 다음 카멜레온 스킨/브러시팩 등 코스메틱(원가 0, 순수 마진).
- 2차: "일일 챌린지" 리텐션 → 규모 확보용(직접 과금 아님).

## 보안 체크리스트 (해당 항목만)
- **시크릿 하드코딩 금지**: API 키·엔드포인트 없음(백엔드 0). 애널리틱스는 vs-arena처럼 공개 스니펫만.
- **유저 입력**: 서버로 안 감(전부 클라이언트). 공유 URL의 `seed`는 **정수 파싱+범위 clamp**로 검증(문자열 그대로 안 씀).
- **XSS**: 점수/시드 등 동적 값은 `textContent`로만 DOM에 넣는다. `innerHTML`에 사용자값 금지(coding-rules SEC-2).
- **공통**: HTTPS 전제(GitHub Pages 기본), 에러에 내부정보 노출 없음.
- (로그인·PII·결제 없음 → 해당 항목 없음.)

## 기술스택
- **바닐라 JS + Canvas 2D + HTML/CSS**, 빌드 스텝 0, **GitHub Pages** 배포. 근거: 사용자 기존 역량·vs-arena와 동일 스택, 캔버스 픽셀 조작(`getImageData`)이 위장점수 알고리즘의 핵심이라 프레임워크 불필요. 의존성 0 = 로딩 즉시.
- 사운드: 절차적 WebAudio(에셋 0). 시드 RNG: mulberry32(vs-arena와 동일 패턴).
- 멀티플레이(로드맵)만 WebSocket(Cloudflare Workers/Durable Objects) 필요 — MVP에선 안 씀.

## 활용 스킬
- **verify/preview — Phase 2 실행검증**: 브라우저에 띄워 스캔·점수·공유 동작 확인, 스크린샷.
- **code-review — Phase 2 마감**: 위장점수 루프·픽셀 처리의 정확성/성능 리뷰.
- **canvas-design — og:image/썸네일**: 쇼츠 공유용 링크 프리뷰 이미지(1200×630) 필요 시.

## MVP 범위
**IN (이번에 만듦)**
- 시드 기반 절차적 장면(색 존 2~3개: 하늘/땅/잎사귀 등) + 카멜레온 실루엣 배치(존을 가로질러 → 그라데이션 위장이 스킬이 됨)
- 스포이트(배경색 찍기) + 브러시(크기 조절) + 자동 추출 팔레트 스와치로 몸에 칠하기
- 제한시간 → "숨기" 버튼/타임아웃 시 **술래 스캔**: 픽셀 diff 위장점수(%) 계산, 스캔 애니메이션
- 판정: 임계치 이상=숨었다 / 미만=들켰다(가장 튀는 부위 마커) + 점수·등급
- 공유: `?seed=` 링크 복사 + 스크린샷, 리플레이/다음 장면
- 절차적 사운드, 모바일 세로 대응

**OUT (로드맵)**
- 실시간 멀티(술래=사람), 3D, 계정/랭킹 서버, 스킨 상점, 레벨 진행, 패턴(무늬) 매칭 정밀도

## 로드맵
1. **일일 챌린지 시드 + 로컬 베스트/공유 랭킹**(리텐션) → 규모 확보
2. **장면 다양화 + 난이도**(무늬 매칭, 이동하는 술래 시야) + 애드슬롯 활성화
3. **실시간 멀티**(술래 vs 숨는 사람들, WebSocket) + 코스메틱 상점
